server:
  port: 8085
spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
  redis:
    port: 6379
    host: 119.29.243.22
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/